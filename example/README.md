# Elm Zen CSS Bootstrap example

Make sure you run all the following commands from within the `example` directory.

Start by installing the dependencies:

```
yarn
```

Then run the following command:

```
yarn run live
```